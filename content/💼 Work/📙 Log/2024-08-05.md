---
projects:
  - Axial pressure torque
---

- when `simple_buoyancy = 0` ==for a magnetic problem==, having `BV2 = 0` or `!=0` affects the ability to obtain solutions even though it shouldn't.
- The matrices are **identical** in a low resolution example (`E=1e-3`).

The issue seems to have resolved itself... -> maybe `BV2` wasn't the problem and there is another issue with the solver.

- On the other hand, I've managed to get pretty decent plots without the magnetic field. 

- ==I wonder if the issue of the `nan's` when there is simple buoyancy at low Ekman might be due to the small thickness of the upper stratified layer where the motion takes place.==